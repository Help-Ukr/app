import { createTheme } from '@mui/material';

export const themeOptions = createTheme({
    palette: {
        mode: 'dark',
        primary: {
            main: '#393939',
        },
        secondary: {
            main: '#FFD500',
        },
        background: {
            paper: '#262626',
        },
    },
});
